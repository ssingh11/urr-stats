services:
  - type: web
    name: urr-stats
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DB_PATH
        value: /data/urr_stats.db
    disk:
      name: urr-data
      mountPath: /data
      sizeGB: 1
